# Component F runtime dependencies.
# boto3 ships in the AWS Lambda Python base image; pinned so the built image and
# pip-audit (DEC-8) have a known version. USAspending is called with stdlib urllib
# (no HTTP client dependency); the source is public and keyless (no secret, cf DEC-7).
boto3>=1.43.45,<2
