# Component A runtime dependencies.
# boto3/botocore ship in the AWS Lambda Python base image; pinned here so the
# built image and pip-audit (DEC-8) have a known version. No powertools — the
# idempotency mechanism is hand-rolled and visible in app.py (DEC-13).
boto3>=1.43.41,<2
