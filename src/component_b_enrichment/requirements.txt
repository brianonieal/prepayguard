# Component B runtime dependencies.
# boto3 ships in the Lambda base image; pinned for the build + pip-audit (DEC-8).
# Matching uses only the standard library (difflib, re) - no extra deps.
boto3>=1.43.50,<2
