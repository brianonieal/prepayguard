# Component E runtime dependencies.
# boto3/botocore ship in the AWS Lambda Python base image; pinned here so the
# built image and pip-audit (DEC-8) have a known version. The idempotency claim
# mirrors Component A's hand-rolled state machine (DEC-13/DEC-16) - no powertools.
boto3>=1.34,<2
# v2.1.2: parse Excel (.xlsx) batch files. Pure-Python, Lambda-friendly.
openpyxl>=3.1,<4
